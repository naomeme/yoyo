<div class="form_wrap">
<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :subject, :placeholder => "subject", :class => "nodes", :size => "50" %>

  </div>
  <div class="field">
    <%= f.text_field :name,:placeholder => "name",:class => "nodes", :size => "50" %>

  </div>
  <div class="field">
    <%= f.text_area :body,:size => "50x10" %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn" %>
  </div>
<% end %>
</div>